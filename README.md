# Hierarchical Combobox (Async, Virtualized, Multi-Select)

A fully accessible, keyboard-first hierarchical combobox built from scratch using
React, TypeScript, Tailwind CSS, and Storybook.

## ✨ Features
- Async tree loading
- Manual virtualization (no libraries)
- Multi-select with indeterminate states
- Search with ancestry context
- Full keyboard navigation
- Screen reader support
- Storybook-driven development

## 🧱 Tech Stack
- React 18
- TypeScript (strict)
- Tailwind CSS (Vite plugin)
- Storybook
- Testing Library
- axe-core

## 🚫 Constraints
No component libraries, no virtualization libs, no select/combobox libs, no AI builders.

## 📦 Development
```bash
npm install
npm run dev
npm run storybook
# Hierarchical Combobox (Async, Virtualized, Multi-Select)

A fully accessible, keyboard-first hierarchical combobox built from scratch using
React, TypeScript, Tailwind CSS, and Storybook.

## ✨ Features
- Async tree loading
- Manual virtualization (no libraries)
- Multi-select with indeterminate states
- Search with ancestry context
- Full keyboard navigation
- Screen reader support
- Storybook-driven development

## 🧱 Tech Stack
- React 18
- TypeScript (strict)
- Tailwind CSS (Vite plugin)
- Storybook
- Testing Library
- axe-core

## 🚫 Constraints
No component libraries, no virtualization libs, no select/combobox libs, no AI builders.

## 📦 Development

npm install
npm run dev
npm run storybook
